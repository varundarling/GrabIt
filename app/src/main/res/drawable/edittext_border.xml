<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Transparent fill -->
    <stroke
        android:width="2dp"
    android:color="#B0BEC5" /> <!-- Light gray color (adjustable, e.g., #607D8B for darker) -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="20dp" /> <!-- Comfortable padding -->
</shape>